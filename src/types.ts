export type Theme = "dark" | "light" | "system";
export type Status = "started" | "stoped" | "data";
export type UserData = {
  _id: string;
  email: string;
  name: string;
  avatar: string;
  role: string;
};
